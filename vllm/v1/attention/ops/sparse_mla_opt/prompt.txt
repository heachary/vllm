I want to optimize this kernel :  @vllm-upstream/vllm/vllm/v1/attention/ops/rocm_aiter_mla_sparse.py:1699-1719  I have a test: @vllm-upstream/vllm/vllm/v1/attention/ops/sparse_mla_opt/test_sparse_mla.py  which you should use to benchmark the performance of this kernel for different shapes / scenarios seen in the end to end run. I want to focus on batch size 4 and improve the kernel performance for both csa and hca modes

There is already an improved version of the kernel at @vllm-upstream/vllm/vllm/v1/attention/ops/sparse_mla_opt/rocm_aiter_mla_sparse_v1.py 

Benchmark the kernel -> establish a perf baseline -> optimize -> bench again to see the performance. 

I want to get atleast a 10x improvement in runtime over the baseline. Also bench v1. If v1 improves performance over the baseline, you can start the optimization from there. Continously iterate with different versions of the kernel until you hit the required performance.

Use the thread level trace from rocprofv3 to look at thread level information and understand where the bottlenecks are and optimize them further (https://rocm.docs.amd.com/projects/rocprofiler-sdk/en/develop/how-to/using-thread-trace.html ). Use the thread level traces to understand where pipeline stalls take place. Reimplement the kernel such that bottlenecks are improved - I dont want just cosmetic changes like modifying the tile size to increase occupancy. 

You can add the optimized kernel in the sparse_mla_opt folder under a rocm_aiter_mla_sparse_v2.py file.